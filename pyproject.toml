[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[project]
name = "crypto-processing-api"
version = "0.2.0"
description = "Single-tenant custodial crypto payment and ledger service on top of BTCPay Server"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.12"
authors = [{ name = "crypto-processing-api contributors" }]
keywords = ["bitcoin", "btcpay", "custodial", "ledger", "double-entry", "usdt"]
classifiers = [
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Office/Business :: Financial",
]

dependencies = [
    "fastapi==0.141.1",
    "uvicorn[standard]==0.52.1",
    "pydantic==2.13.4",
    "pydantic-settings==2.15.0",
    "SQLAlchemy==2.0.51",
    "psycopg[binary]==3.3.4",
    "alembic==1.19.1",
    "httpx==0.28.1",
    "structlog==26.1.0",
]

[project.optional-dependencies]
dev = [
    "hypothesis==6.165.2",
    "pytest==9.1.1",
    "pytest-cov==7.1.0",
    "ruff==0.16.2",
    "mypy==2.3.0",
]

[project.urls]
Homepage = "https://github.com/OliverD25/crypto-processing-api"
Documentation = "https://github.com/OliverD25/crypto-processing-api/tree/main/docs"

[project.scripts]
crypto-processing-api = "crypto_processing_api.cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/crypto_processing_api"]

[tool.ruff]
line-length = 100
target-version = "py312"
# `sdks/python` is here so the SDK's own package sorts as first-party in its
# tests, the same way `src` does for this one.
src = [".", "src", "sdks/python"]
# The design documents are a historical record of what was decided and why.
# Reformatting their code sketches would rewrite the record.
extend-exclude = [
    "docs",
    # Generated code, and the style is the generator's business. Linting it
    # would mean either hand-fixing output that the next regeneration
    # overwrites, or loosening rules that the handwritten code should keep.
    "sdks/python/crypto_processing_client/_generated",
    "sdks/python/crypto_processing_client/event_types.py",
]

[tool.ruff.lint]
select = [
    "E",   # pycodestyle errors
    "W",   # pycodestyle warnings
    "F",   # pyflakes
    "I",   # isort
    "B",   # bugbear
    "UP",  # pyupgrade
    "C4",  # comprehensions
    "SIM", # simplify
    "RUF",
    "S",   # bandit
]
ignore = [
    "S101", # asserts are how the ledger states its invariants
]

[tool.ruff.lint.flake8-bugbear]
# FastAPI's whole dependency mechanism is a call in an argument default.
extend-immutable-calls = [
    "fastapi.Depends",
    "fastapi.Query",
    "fastapi.Path",
    "fastapi.Header",
    "fastapi.Body",
]

[tool.ruff.lint.per-file-ignores]
"tests/**" = ["B008", "S105", "S106", "S107", "S311"]
# The SDK's tests carry the same obviously-fake keys and secrets as this one's.
"sdks/python/tests/**" = ["S105", "S106", "S107"]
# S608: the dev smoke test builds throwaway psql queries from values it
# generated itself, against a regtest database it also created.
"scripts/dev/**" = ["S105", "S106", "S608"]
"scripts/**" = ["S105", "S106"]
"migrations/versions/*" = ["E501"]

[tool.mypy]
python_version = "3.12"
strict = true
warn_unreachable = true
plugins = []
# `verify_nile.py` is the one script in here that makes assertions about money
# against a live network, and it runs once, by hand, with an operator waiting.
# A type error found at that moment costs a session; CI is where it belongs.
files = ["src", "scripts/verify_nile.py"]

[[tool.mypy.overrides]]
module = ["alembic.*"]
ignore_missing_imports = true

[tool.pytest.ini_options]
testpaths = ["tests"]
# The suite imports "tests.fakes" etc.; an editable install happens to put the
# repo root on sys.path, a plain "pip install" (CI) does not.
pythonpath = ["."]
addopts = "-ra --strict-markers"
markers = [
    "integration: needs a real PostgreSQL instance (deploy/docker-compose.test.yml)",
    "no_ledger_check: skip the post-test derived-vs-materialized assertion",
]

[tool.coverage.run]
branch = true
source = ["src/crypto_processing_api"]

[tool.coverage.report]
exclude_also = [
    "if TYPE_CHECKING:",
    "raise NotImplementedError",
]
